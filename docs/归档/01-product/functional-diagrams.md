# 功能示意图

面向：老师评审、产品讨论、开发联调  
整理日期：2026-07-12  
说明：图示采用 Mermaid 编写，可在支持 Mermaid 的 Markdown 工具中直接渲染；如果 NAS 预览不渲染，可按图中结构转换为 PNG/PDF。

## 1. 平台整体功能结构

```mermaid
flowchart TD
    A[统一登录与权限] --> B[角色化看板]
    B --> C[成员情况]
    B --> D[项目情况]
    B --> E[设备与物资]
    B --> F[会议与日常工作]
    B --> G[AI 助手]
    D --> D1[项目目录]
    D1 --> D2[项目详情]
    D2 --> D3[项目树与任务]
    D2 --> D4[项目笔记与资料]
    D2 --> D6[项目文档地址]
    D2 --> D7[项目仓库地址]
    D2 --> D5[进度汇报与教师点评]
    E --> E1[物品目录]
    E1 --> E2[申请与审批]
    E2 --> E3[借用/归还/逾期]
    E3 --> E4[库存流水与存放位置]
    F --> F1[会议通知]
    F1 --> F2[参会/请假]
    F2 --> F3[会议纪要]
    G --> G1[知识库问答]
    G --> G2[项目上下文问答]
    G --> G3[云端/本地模型 Provider]
    D4 --> H[NAS 文件资料]
    G1 --> H
```

## 2. 管理者多级下钻示意图

```mermaid
flowchart LR
    A[全局看板]
    A --> B[成员情况]
    A --> C[项目情况]
    A --> D[物资（含耗材、器材等）情况]
    A --> E[会议情况]
    A --> F[日常工作]
    B --> B1[年度]
    B1 --> B2[组别]
    B2 --> B3[成员]
    B3 --> B4[所属项目]
    B4 --> B5[任务/成果/资料]
    C --> C1[年度]
    C1 --> C2[项目等级]
    C2 --> C3[项目归属/负责人]
    C3 --> C4[项目详情]
    C4 --> C5[成员/进度/物资/纪要]
    D --> D1[总库]
    D1 --> D2[在库/在用/借出/逾期]
    D2 --> D3[高值/低值/类别]
    D3 --> D4[具体物品]
    D4 --> D5[借用人/项目/流水]
    E --> E1[近期/历史]
    E1 --> E2[具体会议]
    E2 --> E3[参会/请假]
    E2 --> E4[纪要/关联项目]
```

## 3. 横向交叉查询示意图

```mermaid
flowchart TD
    M[成员] <--> P[项目]
    P <--> T[任务与进度]
    P <--> F[项目资料/笔记]
    P <--> I[物资申请]
    I <--> R[物品/库存/借用记录]
    P <--> C[会议/纪要]
    C <--> M
    F <--> K[公共知识库]
    X[可配置筛选条件] --> M
    X --> P
    X --> R
    X --> C
    X --> F
```

示例：从成员进入可以查看所属组、参与项目、任务、资料和物资申请；从项目进入可以反查成员、任务进度、资料、会议纪要和物资申请；从物资借用记录进入可以反查借用人、所属组、关联项目和库存流水。

## 4. 普通成员和管理者流程对比

```mermaid
flowchart LR
    subgraph S[普通成员]
        S1[我的看板] --> S2[查看项目/任务]
        S2 --> S3[提交进度/资料]
        S1 --> S4[申请物资]
        S4 --> S5[查看审批与借用]
        S1 --> S6[查看会议/请假]
    end
    subgraph A[管理者]
        A1[管理看板] --> A2[查看成员和项目全局]
        A2 --> A3[下钻到组/人/项目]
        A1 --> A4[审批物资]
        A4 --> A5[库存/借用/逾期追踪]
        A1 --> A6[发布会议/查看参会]
        A6 --> A7[录入纪要并关联项目]
    end
```

## 5. 使用与变更规则

- 图示是需求说明，不代表所有功能已经实现。
- v1.0 优先实现图中的 P0 主链路；动态字段和新增一级目录通过插件/配置扩展。
- 图示每次变更必须同步正式需求文档、功能蓝图和周会进度。
- 上传 NAS 时建议与本文件一同上传教师评审包中的 README、正式需求、目标架构和 v1 开发计划。
